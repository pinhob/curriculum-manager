import { Link } from "react-router-dom";

const AddCurriculumButton = () => {
  return (
    <span><Link to="/curriculo/novo">Adicionar currículo</Link></span>
  )
};

export default AddCurriculumButton;
